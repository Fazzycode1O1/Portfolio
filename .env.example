# --- Database ---
MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/portfolio?retryWrites=true&w=majority

# --- Auth ---
# Generate with: openssl rand -base64 48
JWT_SECRET=replace-with-a-long-random-string-at-least-32-chars

# --- Admin seeding (used by `npm run seed`) ---
ADMIN_SEED_EMAIL=admin@example.com
ADMIN_SEED_PASSWORD=change-me-please

# --- GitHub stats (optional, raises rate limit + enables private stats) ---
GITHUB_USERNAME=Fazzycode1O1
GITHUB_TOKEN=

# --- Email (optional, for contact pipeline) ---
RESEND_API_KEY=
CONTACT_TO_EMAIL=alif61598@gmail.com

# --- Uploads (optional) ---
BLOB_READ_WRITE_TOKEN=

# --- Site ---
NEXT_PUBLIC_SITE_URL=http://localhost:3000
